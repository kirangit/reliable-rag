# Streamlit configuration — light theme by default (per project preference).
[theme]
base = "light"
primaryColor = "#0E7C86"      # cnWave-ish teal accent
font = "sans serif"

[server]
headless = true
